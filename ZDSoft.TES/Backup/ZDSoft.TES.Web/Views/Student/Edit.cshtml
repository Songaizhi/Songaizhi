@model ZDSoft.TES.Domain.Student
@{
    ViewBag.Title = "学生管理—修改信息";
    Layout =null;
    <link href="@Url.Content("~/Content/css/ViewPage.css")" rel="stylesheet" type="text/css" />
}
<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>
<div style=" margin-top:110px;">
<p>
    @Html.ActionLink("返回列表", "Management", "Student")
</p>
@using (Html.BeginForm())
{
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>修改学生</legend>
        @if (Model.Name == "管理员")
        {
            <div class="editor-label">
                @Html.LabelFor(model => model.Name)
            </div>
            <div class="editor-field">
                @Model.Name
                @Html.HiddenFor(model => model.Name)
            </div>}
        else
        {
            <div class="editor-label">
                @Html.LabelFor(model => model.Name)
            </div>
            <div class="editor-field">
                @Html.EditorFor(model => model.Name)
                @Html.ValidationMessageFor(model => model.Name)
            </div>
            
        }
        <div class="editor-label">
            @Html.LabelFor(model => model.No)
        </div>
        <div class="editor-field">
            @Model.No
            @Html.HiddenFor(model => model.No)
        </div>
        <div class="editor-label">
            @Html.LabelFor(model => model.Classes.Name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Classes.Name)
            @Html.HiddenFor(model => model.Classes.Name)
        </div>
        @Html.HiddenFor(model => model.ID)
        <p>
            <input type="submit" value="保存" class="btn3" />
        </p>
    </fieldset>
}
</div>

